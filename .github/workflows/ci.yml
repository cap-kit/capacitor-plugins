name: CI

on:
  push:
    branches: [main, 'renovate/**']
    paths-ignore:
      - 'packages/**/*.md'
      - 'docs/**'
      - 'assets/**'
      - 'LICENSE'
      - '.gitignore'
      - '.editorconfig'

  pull_request:
    branches: [main]
    paths-ignore:
      - 'packages/**/*.md'
      - 'docs/**'
      - 'assets/**'
      - 'LICENSE'
      - '.gitignore'
      - '.editorconfig'

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Required for diffing against origin/main

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: 'https://registry.npmjs.org' # CapKit registry protocol
          cache: pnpm

      - run: pnpm install --frozen-lockfile # Prevents lockfile drift in CI

      - name: Verify Root README sync
        run: |
          pnpm run readme:update
          git diff --exit-code README.md || (
            echo "❌ Root README.md is out of sync with public packages." &&
            echo "Run 'pnpm run readme:update' and commit the changes." &&
            exit 1
          )

      - name: Lint (main)
        if: github.ref == 'refs/heads/main'
        run: pnpm lint

      - name: Lint (impacted)
        if: github.ref != 'refs/heads/main'
        run: pnpm --filter "[origin/main]..." lint

  build-test:
    name: Build and Test
    runs-on: macos-latest # Required for xcodebuild and iOS tooling
    needs: lint

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: 'https://registry.npmjs.org'
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # Detect impacted packages (early exit optimization)
      - name: Detect impacted packages
        id: affected
        run: |
          echo "Checking for impacted packages..."
          pnpm --filter "[origin/main]..." list --depth -1 > affected.txt
          if [ ! -s affected.txt ]; then
            echo "No impacted packages detected."
            echo "none=true" >> $GITHUB_OUTPUT
          else
            echo "Impacted packages:"
            cat affected.txt
            echo "none=false" >> $GITHUB_OUTPUT
          fi

      - name: Build (main)
        if: github.ref == 'refs/heads/main'
        run: pnpm build

      - name: Build (impacted)
        if: github.ref != 'refs/heads/main' && steps.affected.outputs.none != 'true'
        run: pnpm --filter "...[origin/main]" build

      - name: Test (main)
        if: github.ref == 'refs/heads/main'
        run: pnpm test --workspace-concurrency=1

      - name: Test (impacted)
        if: github.ref != 'refs/heads/main' && steps.affected.outputs.none != 'true'
        run: pnpm --filter "[origin/main]..." test --workspace-concurrency=1
