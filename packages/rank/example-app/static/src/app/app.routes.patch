--- a/src/app/app.routes.ts
+++ b/src/app/app.routes.ts
@@ -6,5 +6,10 @@
       import('./home/home.page').then((m) => m.HomePage),
   },
+  {
+    path: 'rank',
+    loadComponent: () =>
+      import('./rank/rank.page').then((m) => m.RankPage),
+  },
   {
     path: '',
     redirectTo: 'home',
