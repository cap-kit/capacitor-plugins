--- a/src/app/app.routes.ts
+++ b/src/app/app.routes.ts
@@ -6,6 +6,11 @@
       import('./home/home.page').then((m) => m.HomePage),
   },
+  {
+    path: 'redsys',
+    loadComponent: () =>
+      import('./redsys/redsys.page').then((m) => m.RedsysPage),
+  },
   {
     path: '',
     redirectTo: 'home',
