--- a/capacitor.config.ts
+++ b/capacitor.config.ts
@@ -1,3 +1,5 @@
+/// <reference types='@cap-kit/tls-fingerprint' />
+
 import type { CapacitorConfig } from '@capacitor/cli';
 import { KeyboardResize } from '@capacitor/keyboard';
 
@@ -25,6 +27,51 @@
     Keyboard: {
       resize: KeyboardResize.None,
     },
+    TLSFingerprint: {
+      /**
+       * Enables verbose native logging.
+       * Logs are printed in Logcat (Android) and Xcode console (iOS).
+       * Default: false
+       */
+      verboseLogging: false,
+      /**
+       * Default SHA-256 fingerprint used by checkCertificate()
+       * when no fingerprint is provided at runtime.
+       *
+       * Format:
+       * - SHA-256
+       * - Hexadecimal
+       * - With or without colon separators (both supported)
+       */
+      fingerprint:
+        '3B:A9:2F:06:7A:07:3E:03:79:CC:79:0E:D9:81:92:D7:42:A0:47:45:1A:3C:CF:88:E5:A1:95:FD:2A:A8:86:8A',
+      /**
+       * Default SHA-256 fingerprints used by checkCertificates()
+       * when no fingerprints are provided at runtime.
+       *
+       * A match succeeds if ANY fingerprint matches.
+       */
+      fingerprints: [
+        '23:93:29:74:84:7F:D8:B3:0A:7A:9C:FC:19:0B:AD:83:11:19:3D:87:ED:AB:2E:2F:E1:7F:6C:D3:B2:E9:C9:8E',
+      ],
+      /**
+       * Domains excluded from fingerprint comparison.
+       *
+       * Behavior:
+       * - TLS connection is still performed
+       * - Leaf certificate is still extracted
+       * - Fingerprint comparison is skipped
+       * - Result mode = 'excluded'
+       *
+       * Matching rules:
+       * - Exact domain match
+       * - Subdomain match
+       *   (e.g., 'example.com' matches 'api.example.com')
+       *
+       * Do NOT include protocol or paths.
+       */
+      excludedDomains: ['google-analytics.com', 'sentry.io', 'doubleclick.net']
+    },
   },
   cordova: {},
 };
