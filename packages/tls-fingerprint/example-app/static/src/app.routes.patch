--- a/src/app/app.routes.ts
+++ b/src/app/app.routes.ts
@@ -6,6 +6,11 @@
       import('./home/home.page').then((m) => m.HomePage),
   },
+  {
+    path: 'tls-fingerprint',
+    loadComponent: () =>
+      import('./tls-fingerprint/tls-fingerprint.page').then((m) => m.TLSFingerprintPage),
+  },
   {
     path: '',
     redirectTo: 'home',
